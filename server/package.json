{
  "name": "kaalerto-server",
  "version": "0.1.0",
  "private": true,
  "description": "Event sync server for KaAlerto. Aggregates and accelerates; it is never the authority — see docs/03-architecture.md.",
  "type": "commonjs",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "5.2.1"
  }
}
