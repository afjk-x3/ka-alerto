# Context handoff — KaAlerto, 19 Sep 2026 (end of session)

Read `CLAUDE.md` first (project rules, decisions not to re-litigate). This file only covers the state at the end of this session and what to do next.

## State of the repo

- Branch `feat/event-sourced-roles`, `main` and `origin/*` are all at `ec58bda`. Working tree is clean except the untracked `HANDOFF-passable-v0.md` and this file. Leave both uncommitted unless asked.
- Solo developer, git author `afjk.ampol`. **Never add a Claude co-author trailer or "Generated with Claude Code" line** to commits or PRs (pinned memory `no-claude-coauthor`). About 89 older pushed commits still carry it; do not force-push `main` to remove them unless the user explicitly asks.
- **Commit and push only when the user asks**, one commit per fix. Pushing means: push `feat/event-sourced-roles`, then fast-forward `main` to it and push `main`.
- Deadline: 30 Sep 2026.

## What was done this session (all committed and pushed)

- **Dashboard** (`dashboard/`, Next.js, light mode only, deployed at https://kaalerto.vercel.app/): PIN gate through `/api/events`, logout, collapsible sidebar, 5 s auto-refresh, sound/notification alerts for S3 reports and SOS, report photos via `/api/photo/[hash]`, directions block (Google Maps from current location plus OSRM safe-route alternatives ranked by nearby flood reports), beacon logo and favicon.
- **Backend:** the Node server is deleted. Supabase is the only backend. Phone-side server sync, LAN discovery and the server-address field are removed.
- **Sync:** WorkManager background push, 30 s loop that backs off to 2 min after 3 failures (`nextSyncDelayMs`), and a purge guard (`isAwaitingUpload`, `PushState`, `last_full_push_ok_ms`) that keeps an expired, not-yet-uploaded report until a full push succeeds. No Room schema change.
- **App:** beacon launcher icon (fits the adaptive-icon safe zone), Xiaomi/Redmi/Poco background tip banner (`map/BackgroundTip.kt`), last "server" wording removed.
- **Release + CI:** `android/keystore.properties` and `android/kaalerto-release.jks` (both gitignored, dev machine only) sign `assembleRelease`; without them the build is `app-release-unsigned.apk`. `.github/workflows/ci.yml` runs Android unit tests plus debug/release builds and the dashboard type-check and build. **CI is green** (run on `ec58bda`).
- **Docs:** README, `BUILD_TASKS.md`, `CLAUDE.md` and `android/README.md` updated.

## Decisions to keep (user's calls this session)

- Background upload: **kept**. Access control: **finished, do not touch** (Supabase is open; the dashboard PIN guards only the dashboard). SMS and the day-14 rehearsal / backup video are **out of scope**.
- PRD edits (`submissions/Macci-PRD.md` and gitignored `docs/02-prd.md`) need proposed wording and an explicit yes first.
- Release APK is signed with the new key, so it will not install over the v0 GitHub release APK (debug-signed) without an uninstall. **Back up `kaalerto-release.jks` and `keystore.properties`**; a lost key means every user must uninstall to update.

## Device and tooling notes

- Real phone: Xiaomi, adb serial `711c291c`. Always use `adb -s 711c291c`; with Git Bash set `MSYS_NO_PATHCONV=1` for `/sdcard` paths. Debug package is `com.macci.kaalerto.debug`. Camera preview is black in screencap.
- Build from `android/`: `export JAVA_HOME="/c/Program Files/Android/Android Studio/jbr"`, then `./gradlew testDebugUnitTest assembleDebug`. Check the APK mtime before installing.
- Long Git Bash heredocs fail; write scripts with the Write tool. Many files are CRLF, so use CRLF-aware edits.
- Use `python -m graphify query "<question>"` before raw grep (project rule).
- The empty `server/` folder cannot be removed on Windows ("busy"); git ignores it.

## Not verified yet

- Back-off timing on a device (unit test only); purge guard against a real expired report (predicate unit-tested, delete path only cold-started).
- The new strings "Kinuha online" and the Family subtitle on screen.
- Detail-sheet bounce (not reproduced, watching), BLE-only mesh range, QR pairing between two phones, dashboard on a narrow screen, dashboard alarm sound.
- The release APK has not been installed on the phone.

## Next: remaining upgrades (user said "we are not yet done, still half way")

The user has not yet picked. Recommended order: 10, 8, 7, 6, 5, 9.

10. Narrow-screen check of the dashboard (quick).
8. Dashboard filters (severity, SOS state, age) and CSV export.
7. "Confirm nearby" prompt to residents near a fresh report; raises confidence by real corroboration only (a name never raises confidence).
6. Withdraw your own report: new event type that must fold deterministically on every device (NFR-4). Product change, so present the design and get approval first.
5. In-app routes to a report or SOS (the phone currently opens a plain map link).
9. Evacuation centre status (capacity, open/closed) on dashboard and phone.

Ask the user which to start, or begin with 10 and 8 if they say "your order".
